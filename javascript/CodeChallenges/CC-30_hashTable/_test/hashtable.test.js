'use strict';

let Hasheesh = require('../hashtable');

describe('Hash', () => {
  it('', () => {
    let pineappleHash = new Hasheesh();
    pineappleHash.set();
    expect(pineappleHash.get('')).toEqual('');
  });
  it('', () => {
  });
});
